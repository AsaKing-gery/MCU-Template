# 格式检查 / 循环包含检查要跳过的路径。
#
# 一行一条，只能整行注释（行首 #），不要写行尾注释。
#   Drivers/      结尾带斜杠 = 跳过这个目录
#   arm_math.h    不带斜杠 = 按文件名匹配（可用通配符，如 stm32u5xx_ll_*.h）
#
# 为什么要跳过厂商代码：
#   * 以后没法再和上游版本对比
#   * CubeMX 重新生成代码时会产生大量无意义的 diff
#   * 厂商代码通常是空格缩进，强制转 Tab 会让每一行都算改动
#     （实测某个工程：不排除是 +114461 行，排除后降到 +18282 行）
#
# 这个文件是 tools/format/format-all.ps1 和 tools/check/check-includes.ps1 共用的。
# 判断标准：这段代码不是你写的、而且以后还会被上游覆盖 -> 写进来。

# ── 通用（STM32 CubeMX 工程的常见位置）──
Drivers/
Middlewares/

# ── 按你的工程补在这里，例如 ──
# User/Src/Libraries/
# sd_card/FATFS/
# arm_math.h
